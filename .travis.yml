language: python
python:
  - 2.6
  - 2.7
before_install:
  - sudo apt-get install -qq tcl
before_script:
  - python setup.py develop
  - easy_install pymongo pytest jaraco.test
# command to run tests
script: py.test -rs -s
